extends ../shared/template
block content
  h1 Tasks

  table.table.table-striped
    thead
      tr
        th Id
        th Name
        th Due
        th Priority
        th Category
        th Complete?
        th Created
    tbody
      each task in viewTasks
        tr(style='background-color:' + task.priority.color)
          td
            form(action='/tasks/' + task.id + '/' + task.isComplete + '/complete', method='post')
                button(type='submit') complete
          td= task.name
          td= task.due
          td= task.priority.name
          td= task.category
          td
            if task.isComplete
              i.fa.fa-check-square-o.fa-fw
            else
              i.fa.fa-square-o.fa-fw
          td= task.createdAt
